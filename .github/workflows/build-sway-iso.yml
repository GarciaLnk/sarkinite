name: Sarkinite (Sway) ISO
on:
  workflow_dispatch:

jobs:
  build:
    name: Sarkinite (Sway)
    uses: ./.github/workflows/reusable-build-iso.yml
    secrets: inherit
    with:
      brand_name: sarkinite-sway
      fedora_version: stable
